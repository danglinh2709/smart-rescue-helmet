import type { ReactNode } from 'react'
type Section = 'overview' | 'devices' | 'events' | 'history' | 'system'; const links: { id: Section; label: string }[] = [{ id: 'overview', label: 'Overview' }, { id: 'devices', label: 'Devices' }, { id: 'events', label: 'Events' }, { id: 'history', label: 'History' }, { id: 'system', label: 'System' }]
export function Shell({ section, navigate, wsStatus, deviceCount, children }: { section: Section; navigate: (path: string) => void; wsStatus: string; deviceCount: number; children: ReactNode }) { return <div className="app-shell"><aside className="sidebar"><div className="brand"><span className="brand-mark" aria-hidden="true">SR</span><div><strong>SMART RESCUE</strong><small>Helmet Command Center</small></div></div><nav aria-label="Dashboard navigation">{links.map((link) => <button key={link.id} className={section === link.id ? 'nav-link active' : 'nav-link'} aria-current={section === link.id ? 'page' : undefined} onClick={() => navigate(link.id === 'overview' ? '/' : `/${link.id}`)}>{link.label}</button>)}</nav></aside><main className="content"><header className="topbar"><div><p className="eyebrow">COMMAND CENTER</p><h1>SMART RESCUE HELMET</h1></div><div className="top-status"><span>Devices: <strong>{deviceCount}</strong></span><span>WebSocket: <strong>{wsStatus}</strong></span></div></header>{children}</main></div> }
