import { EventTable } from '../components/EventTable'; import type { DeviceEvent } from '../types/dashboard'
export function EventsPage({ events }: { events: DeviceEvent[] }) { const critical = events.find((event) => event.severity === 'CRITICAL'); return <section><div className="page-heading"><div><h2>Events</h2><p>Safety events persisted by the backend and updated in realtime.</p></div></div>{critical && <div className="critical-alert" role="alert"><strong>CRITICAL ALERT</strong><span>{critical.device_id} · {critical.event_type} · {new Date(critical.timestamp).toLocaleString()}</span></div>}<EventTable events={events} /></section> }
