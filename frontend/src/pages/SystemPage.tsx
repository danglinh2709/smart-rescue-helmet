import type { DeviceState } from '../types/dashboard'
export function SystemPage({ devices, wsStatus, apiOnline }: { devices: DeviceState[]; wsStatus: string; apiOnline: boolean }) { return <section><div className="page-heading"><div><h2>System</h2><p>Connection status reported by the current backend and device data.</p></div></div><div className="system-grid"><article><h3>Backend</h3><p className={apiOnline ? 'good' : 'bad'}>{apiOnline ? 'ONLINE' : 'UNAVAILABLE'}</p></article><article><h3>WebSocket</h3><p>{wsStatus}</p></article></div><h3>Device MQTT state</h3>{devices.length ? <div className="table-wrap"><table><thead><tr><th>Device</th><th>Device Status</th><th>MQTT</th><th>Wi-Fi</th></tr></thead><tbody>{devices.map((device) => <tr key={device.device_id}><td>{device.device_id}</td><td>{device.device_status}</td><td>{device.latest_health?.mqtt ?? device.latest_telemetry?.mqtt ?? 'Unavailable'}</td><td>{device.latest_health?.wifi ?? device.latest_telemetry?.wifi ?? 'Unavailable'}</td></tr>)}</tbody></table></div> : <p className="empty-state">No runtime device states available.</p>}</section> }
