import { DeviceCard } from '../components/DeviceCard'; import type { DeviceState } from '../types/dashboard'
export function OverviewPage({ devices, navigate }: { devices: DeviceState[]; navigate: (path: string) => void }) { const online = devices.filter((device) => device.device_status === 'ONLINE').length; const warning = devices.filter((device) => device.risk_level === 'WARNING').length; const critical = devices.filter((device) => device.risk_level === 'CRITICAL').length; return <><section className="summary-grid" aria-label="Device summary"><article><span>Total Devices</span><strong>{devices.length}</strong></article><article><span>Online Devices</span><strong>{online}</strong></article><article><span>Warning Devices</span><strong>{warning}</strong></article><article><span>Critical Devices</span><strong>{critical}</strong></article></section><section className="page-heading"><div><h2>Live device overview</h2><p>Latest data is received through the backend realtime channel.</p></div></section>{devices.length ? <section className="card-grid">{devices.map((device) => <DeviceCard key={device.device_id} device={device} onOpen={() => navigate(`/devices/${device.device_id}`)} />)}</section> : <p className="empty-state">No runtime devices are available yet.</p>}</> }
